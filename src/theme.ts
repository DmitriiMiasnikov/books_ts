import { DefaultTheme } from "styled-components";

export enum colors {
  white = '#000',
  black = '#fff',
  whiteF0 = '#f0efeb',
  brownED = '#eddcd2',
  blue99 = '#99c1de'
}

const theme: DefaultTheme = {
  colors,
  headerHeight: '4rem',
}

export default theme;